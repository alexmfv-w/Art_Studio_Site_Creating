# Ручная проверка каркаса перед следующим этапом

- [ ] Skip-link: первый Tab на любой странице показывает «Перейти к содержимому», Enter переносит фокус на `<main>`
- [ ] Клавиатурная навигация: Tab проходит по всем ссылкам шапки в видимом порядке слева направо
- [ ] Мобильное меню (окно уже 768px): кнопка ☰ видна, клик открывает меню, повторный клик или клик вне меню — закрывает
- [ ] Мобильное меню через клавиатуру: Tab доходит до кнопки ☰, Enter открывает, Tab продолжает по пунктам меню, Esc закрывает
- [ ] `aria-expanded` на кнопке ☰ в DevTools (вкладка Elements) меняется с `false` на `true` при открытии — без единой строчки нашего JS для этого
- [ ] Блок «Взрослым/Детям»: на широком экране — две колонки, на узком (< 768px) — одна под другой на всю ширину
- [ ] Контраст текста: `--ink-soft` на `--paper` и `--card` — проверить любым онлайн-контраст-чекером на соответствие 4.5:1
- [ ] Все 4 ссылки навигации шапки, 2 ссылки в футере («О студии», «Контакты») и 2 кнопки блока «Взрослым/Детям» (всего 6 уникальных маршрутов, охватывающих все страницы сайта) ведут на реальные страницы без 404

---

## Результаты прохода (2026-08-06)

Проход выполнялся без интерактивного браузера (агент без возможности реального клика/ресайза/скринридера). Ниже — честный статус по каждому пункту: что подтверждено статической проверкой (сборка `npm run build` + разбор сгенерированного `dist/`), а что требует живой браузерной сессии и отмечено как неподтверждённое.

1. **Skip-link (Tab → «Перейти к содержимому» → Enter → фокус на `<main>`).**
   Не проверяемо без реальной браузерной сессии — подтверждена только структурная предпосылка: в `dist/index.html` (и на всех 6 остальных страницах) первый интерактивный элемент в `<body>` — `<a href="#content" class="skip-link">Перейти к содержимому</a>`, целевой элемент `<main id="content" tabindex="-1">` присутствует и имеет `tabindex="-1"` (обязательное условие, чтобы `<main>` мог принять программный фокус через якорь). CSS в `global.css`/собранном `<style>` показывает `.skip-link { position:absolute; top:-100% }` и `.skip-link:focus { top: var(--space-2) }`, то есть ссылка визуально скрыта до фокуса и появляется по `:focus` — это соответствует ожидаемому поведению, но само нажатие Tab/Enter и визуальное появление не наблюдались.

2. **Клавиатурная навигация по ссылкам шапки слева направо.**
   Не проверяемо без реальной браузерной сессии (порядок табуляции — рантайм-поведение). Подтверждена структурная предпосылка: в исходном DOM `dist/index.html` порядок ссылок в `<nav aria-label="Основная" class="nav-desktop">` — Взрослым → Детям → Витрина → Мастер-классы, без `tabindex` или CSS `order`, которые могли бы переставить визуальный/DOM-порядок табуляции. Естественный DOM-порядок при отсутствии `tabindex`/`order` определяет порядок Tab, поэтому проход слева направо ожидаем, но не пронаблюдён вживую.

3. **Мобильное меню мышью (кнопка ☰ видна на ≤768px, клик открывает/закрывает, клик вне — закрывает).**
   Не проверяемо без реальной браузерной сессии (нет возможности реально сузить окно и кликнуть). Подтверждена структурная предпосылка: медиа-запрос `@media (max-width: 768px) { .nav-desktop { display:none } .menu-btn { display:block } }` присутствует в собранном CSS; кнопка использует нативный HTML Invoker Commands API — `<button command="toggle-popover" commandfor="mobile-nav">`, а целевой элемент — `<nav id="mobile-nav" popover ...>`. Открытие/закрытие по клику и закрытие по клику вне попапа (light-dismiss) — встроенное поведение нативного Popover API (`popover` без `popover="manual"` использует auto-режим со light-dismiss), поэтому реализация не требует собственного JS-обработчика клика — в коде компонента (`SiteHeader.astro`) действительно нет такого обработчика. Само визуальное открытие/закрытие не пронаблюдено.

4. **Мобильное меню с клавиатуры (Tab → кнопка ☰ → Enter открывает → Tab по пунктам → Esc закрывает).**
   Не проверяемо без реальной браузерной сессии. Структурно: кнопка `☰` — фокусируемый `<button>`, activation по Enter/Space — стандартное поведение `<button>` при использовании `command`/`commandfor`; закрытие popover по `Esc` — встроенное поведение auto-popover в спецификации Popover API. Полифиллы для обоих API (`invokers-polyfill`, `@oddbird/popover-polyfill`) подключаются условно в `BaseLayout.astro` при отсутствии нативной поддержки (`'commandForElement' in HTMLButtonElement.prototype` / `'popover' in HTMLElement.prototype`), что подтверждено чтением исходника `src/layouts/BaseLayout.astro`. Реальная последовательность нажатий не пронаблюдена.

5. **`aria-expanded` на кнопке ☰ меняется `false → true` при открытии, без своего JS.**
   Не проверяемо без реальной браузерной сессии/DevTools. Подтверждено статически: (а) исходная разметка кнопки не содержит атрибута `aria-expanded` вручную и не содержит `data-*`/скриптовых обработчиков в `SiteHeader.astro`, единственный `<script>` во всём каркасе — условный загрузчик полифиллов в `BaseLayout.astro`, который не трогает `aria-expanded`; (б) синхронизация `aria-expanded` invoker-кнопки с состоянием управляемого popover — задокументированное поведение нативного Invoker Commands API/Popover API (браузер сам выставляет атрибут на элементе-инициаторе). То есть реализация полагается на платформу, как и требует пункт, но фактическое изменение атрибута в панели Elements не пронаблюдено.

6. **Блок «Взрослым/Детям»: две колонки на широком экране, одна колонка на узком (<768px).**
   Частично подтверждено статически, без визуального ресайза. В собранном CSS `.audience-split { display:grid; grid-template-columns: 1fr 1fr }` и `@media (max-width: 768px) { .audience-split { grid-template-columns: 1fr } }` присутствуют дословно (файл `src/components/AudienceSplit.astro` → собранный `<style>` в `dist/index.html`). Правило синтаксически корректно и однозначно задаёт две равные колонки по умолчанию и одну колонку под брейкпоинтом. Фактическое визуальное поведение при живом ресайзе окна не пронаблюдено.

7. **Контраст `--ink-soft` на `--paper` и `--card` ≥ 4.5:1.**
   Проверено расчётом (не через открытый в браузере онлайн-инструмент, но по той же формуле WCAG 2.1 relative luminance/contrast ratio, которую используют такие чекеры): `--ink-soft:#5B6350`, `--paper:#ECEEDF`, `--card:#FAFAF1`.
   - `#5B6350` на `#ECEEDF` → контраст **5.34:1** — проходит порог 4.5:1 (обычный текст).
   - `#5B6350` на `#FAFAF1` → контраст **5.98:1** — проходит порог 4.5:1.
   Расчёт выполнен скриптом по стандартной формуле (sRGB → линейный → относительная яркость → (L1+0.05)/(L2+0.05)); значения не сверялись повторно сторонним онлайн-инструментом, но алгоритм идентичен.

8. **4 ссылки шапки + 2 ссылки футера + 2 кнопки блока «Взрослым/Детям» (6 уникальных маршрутов) ведут на реальные страницы без 404.**
   *Обновлено после исправления (см. `final-fix-report.md`, Fix 3): изначально `/o-studii` и `/kontakty` не имели входящих ссылок нигде на сайте — это было зафиксировано как расхождение в первой версии этого пункта. Исправлено добавлением `<nav aria-label="Разделы">` с ссылками на `/o-studii` и `/kontakty` в `src/components/SiteFooter.astro`.*
   Проверено сборкой и статическим разбором `dist/`. `npm run build` генерирует 7 страниц без ошибок: `/`, `/vzroslym/`, `/detyam/`, `/vitrina/`, `/master-klassy/`, `/o-studii/`, `/kontakty/` — по одной `index.html` на маршрут, у каждой свой `<title>`.
   Текущая карта ссылок (подтверждена чтением исходников и `dist/index.html`):
   - Шапка (`src/components/SiteHeader.astro`, `<nav class="nav-desktop">`/`<nav class="nav-mobile">`): 4 ссылки — `/vzroslym`, `/detyam`, `/vitrina`, `/master-klassy` (каждая отрисована дважды — в десктопном и мобильном меню, это те же 4 адреса).
   - Блок «Взрослым/Детям» (`src/components/AudienceSplit.astro`): 2 кнопки — `/vzroslym`, `/detyam` (дублируют 2 из адресов шапки).
   - Футер (`src/components/SiteFooter.astro`, новый `<nav aria-label="Разделы">`): 2 ссылки — `/o-studii`, `/kontakty`.
   Итого 6 уникальных внутренних маршрутов достижимы хотя бы одной ссылкой на сайте (`/vzroslym`, `/detyam`, `/vitrina`, `/master-klassy`, `/o-studii`, `/kontakty`), все 6 успешно собираются в `dist/` без ошибок и с уникальным `<title>` — 404 не обнаружено. Реальный клик по каждой ссылке в браузере не выполнялся (см. общее ограничение ниже), но существование и корректность сборки целевых маршрутов подтверждены статически.

### Итог
- Подтверждено статическим анализом (сборка + разбор dist + расчёт контраста): пункты 6 (частично, через CSS-правила), 7 (полностью, расчётом), 8 (полностью, включая проверку после Fix 3 из `final-fix-report.md`).
- Требует живой браузерной сессии, статически подтверждены только структурные предпосылки: пункты 1, 2, 3, 4, 5.
- Ни один пункт не отмечен как пройденный по результатам реального клика/ресайза/скринридера/DevTools — такой сессии не было.

---

## Результаты прохода `/o-studii` (2026-08-07)

Планировалась реальная браузерная проверка через скилл `claude-in-chrome`, но расширение оказалось не подключено в этой сессии (`Browser tools are not available: the Claude in Chrome extension is not set up`) — тот же class ограничения, что и в проходе 2026-08-06. Ниже — то же честное разделение: что подтверждено статически, что нет.

**Подтверждено статически** (сборка `npm run build` + разбор `dist/o-studii/index.html`):
- Структура заголовков: `<h1>О студии`, `<h2>Почему у нас`, пять `<h3>` карточек («Опыт», «Мини-группы», «Обратная связь», «Честная цена», «Отношение»), `<h2>Где и как проходят занятия` — все присутствуют, в этом порядке.
- Ровно 5 элементов `class="trust-card reveal"` — по числу пунктов доверия из плана, ни одного лишнего/недостающего.
- Класс `reveal` (Task 1) действительно применён на вступлении, секции формата и всех 5 карточках (не только объявлен, но и использован).
- CTA-кнопка `<a class="btn" href="/kontakty">Записаться</a>` присутствует и ведёт на существующий маршрут.
- `PhotoPlaceholder` без `src` отрендерен дважды (`photo-placeholder` × 2) — вступление и секция формата, как в плане.
- Секция отзывов отсутствует на странице (0 упоминаний «отзыв») — соответствует решению спеки не выводить её без контента.
- Никаких следов сломанного шаблонизирования (`undefined`, `[object Object]`) в собранном HTML.

**Не проверено вживую** (нужна реальная браузерная сессия — унаследованные ограничения из прохода 2026-08-06, актуальны и здесь):
- Визуальный результат scroll-reveal анимации при прокрутке (появляется ли плавно, а не рывком; работает ли `animation-timeline: view()` в реальном движке или страница просто статична из-за отсутствия поддержки — на момент написания это ожидаемо для Firefox).
- Hover/focus-состояния карточек (`translateY(-2px)`, смена цвета рамки) и кнопки (фон светлеет, `--forest` → `--forest-mid`) — визуально не пронаблюдены, только CSS-правила проверены на синтаксис.
- Реальное поведение адаптивной раскладки intro/format при сужении окна до <768px.
- Поведение при `prefers-reduced-motion: reduce` (должна остаться без анимации и без сдвигов на hover) — не эмулировано в DevTools.

### Итог по `/o-studii`
- Структурная корректность подтверждена статически и достаточно надёжна (сборка чистая, разбор HTML подтверждает все ожидаемые элементы).
- Визуальное поведение анимаций и адаптива — не подтверждено, требует реальной браузерной сессии (подключить `claude-in-chrome` через `/chrome` или проверить вручную).

---

## Результаты прохода `/detyam` (2026-08-07)

`claude-in-chrome` по-прежнему не подключен в этой сессии — то же ограничение, что и на проходе `/o-studii` выше. Статическая проверка сборки и разбор `dist/`.

**Подтверждено статически** (сборка `npm run build` + разбор `dist/detyam/index.html`):
- Структура заголовков: `<h1>Детям`, `<h2>Чем занимаемся` → 4×`<h3>` (Керамика, Лепка и пластилинография, Каллиграфия брашпеном, Скетчинг и леттеринг), `<h2>Почему у нас` → 4×`<h3>` (Опыт, Мини-группы, Обратная связь, Честная цена) — верный порядок, без пропуска уровней.
- Ровно 2 бейджа «8+» — привязаны точно к «Керамика» и «Каллиграфия брашпеном» (проверено разбором HTML на близость к нужному тексту, не только подсчётом), у «Лепка и пластилинография» и «Скетчинг и леттеринг» бейджа нет — соответствует спеке (возраст не был в источнике, не выдуман).
- Обе секции `TrustGrid` используют `--accent: var(--kid)` в собранном HTML — кид-регистр применён, а не унаследованный нейтральный `--forest` со страницы `/o-studii`.
- CTA-кнопка `<a class="btn" href="/kontakty">Записаться</a>` присутствует.
- `PhotoPlaceholder` без `src` отрендерен во вступлении.
- Обратная совместимость `TrustGrid`: `/o-studii/index.html` после расширения компонента по-прежнему показывает `--accent: var(--forest)` и ни одного бейджа — старая страница не сломана новыми пропами.
- Никаких следов сломанного шаблонизирования (`undefined`, `[object Object]`).

**Не проверено вживую** (те же унаследованные ограничения):
- Визуальное поведение scroll-reveal, hover/focus у карточек и кнопки, адаптивная раскладка на <768px, поведение при `prefers-reduced-motion: reduce`.

### Итог по `/detyam`
- Структурная корректность и точность контента (бейджи, кид-акцент, обратная совместимость `TrustGrid`) подтверждены статически.
- Визуальное поведение — не подтверждено, тот же открытый пункт, что и для `/o-studii`.

---

## Результаты прохода `/vzroslym` (2026-08-07)

`claude-in-chrome` по-прежнему недоступен (третья сессия подряд) — та же статическая проверка, что и на предыдущих двух страницах.

**Подтверждено статически** (сборка `npm run build` + `npm run check` + разбор `dist/vzroslym/index.html`):
- Оба билд-гейта чистые: `npm run build` — 7 страниц без ошибок; `npm run check` — 0 ошибок, 0 предупреждений (1 не связанный с этой работой hint, тот же, что и раньше).
- Заголовки в правильном порядке: `<h1>Взрослым`, `<h2>Чем занимаемся` → 4×`<h3>`, `<h2>Почему у нас` → 4×`<h3>`.
- 0 элементов `class="badge"` — на этой странице бейджи намеренно не используются (в отличие от `/detyam`).
- CTA-кнопка на `/kontakty` присутствует, использует `register="adult"` (проверено через переиспользуемый `Button`, без правок самого компонента).
- Никаких следов сломанного шаблонизирования.

**Не проверено вживую**: то же самое, что и для `/o-studii`/`/detyam` — визуальное поведение scroll-reveal, hover, адаптивной раскладки, `prefers-reduced-motion`.

### Итог по `/vzroslym`
Третья страница подряд с одинаковым открытым пунктом — стоит подключить `claude-in-chrome` при следующей возможности и закрыть разом все три накопившихся визуальных пробела, а не откладывать их бесконечно по одному на страницу.

---

## Результаты прохода: типографика (2026-08-08)

Первый цикл пересмотра визуала по спеке `2026-08-08-visual-redesign-direction.md`: системные шрифтовые стеки заменены на самохостимую пару Bitter (заголовки) + Golos Text (текст и лейблы), интерлиньяж основного текста поднят с 1.5 до 1.7.

**Подтверждено статически** (`npm run build`, `npm run check`, разбор `dist/` и запуск `npm run preview`):
- Оба гейта чистые: 7 страниц без ошибок сборки; проверка типов — 0 ошибок, 0 предупреждений (тот же единственный не связанный с этой работой hint в `BaseLayout.astro`).
- В собранном CSS четыре объявления `@font-face` с корректными `unicode-range`: кириллический диапазон `U+400-45F` присутствует, то есть браузер скачает кириллический файл для русского текста.
- Токены указывают на новые гарнитуры с системными запасными: `--font-display: "Bitter", Charter, Cambria, Georgia, serif`, `--font-body` и `--font-label` — `"Golos Text", ui-sans-serif, …`. Если шрифт не догрузился, текст наберётся близкой по характеру гарнитурой, а не дефолтной.
- `line-height:1.7` присутствует в правиле `body`.
- Четыре `.woff2` попадают в `dist/fonts/`, суммарно 124 КБ. На запущенном preview файл шрифта отдаётся с `HTTP 200` и типом `font/woff2`.
- Обращений к `fonts.googleapis.com` / `fonts.gstatic.com` в собранных страницах — ноль, самохостинг полный.

**Не проверено вживую** (`claude-in-chrome` по-прежнему недоступен — пятая сессия подряд):
- **Главное для этого цикла:** не изменился ли зрительный размер заголовков. У Bitter другая высота строчных и другая ширина знака, чем у прежнего Charter, а кегли заданы в пикселях — заголовки могут смотреться крупнее или мельче прежнего, и это видно только глазом.
- Как читается интерлиньяж 1.7 на длинных абзацах в реальном окне (выбран заказчиком по макету, но на живой странице колонка шире).
- Не «частоколит» ли Bitter в длинных русских заголовках — на макете проверено и вопросов не вызвало, но там строки были короче.
- Заметно ли мелькание подмены шрифта (`font-display: swap`) на медленном соединении.

### Итог по типографике
Техническая часть подтверждена полностью: шрифты самохостятся, подмножества верные, внешних зависимостей нет, оба гейта зелёные. Открытый вопрос — исключительно про зрительные пропорции после смены гарнитуры; если заголовки покажутся не того размера, правится в одном месте (`--text-hero`/`--text-h2`/`--text-h3` в `tokens.css`).

---

## Результаты прохода: строка-этикетка и регистры (2026-08-09)

Второй цикл пересмотра визуала. Сетка карточек `TrustGrid` удалена; вместо неё два разных приёма: `LabelList` (строка-этикетка — материалы слева, название и описание в центре, возрастная метка справа, без единой рамки и плашки) для списков направлений и `FounderNote` (связный текст от первого лица с подписью) вместо блока «почему у нас». Детский регистр получил свою пару шрифтов поверх той же структуры.

**Подтверждено статически** (`npm run build`, `npm run check`, разбор `dist/`):
- Оба гейта чистые: 7 страниц, 0 ошибок сборки, 0 ошибок и предупреждений проверки типов (тот же единственный посторонний hint в `BaseLayout.astro`).
- `trust-card` не встречается нигде в `dist/` — старая карточная сетка удалена полностью, мёртвого компонента не осталось (`TrustGrid.astro` удалён, потребителей не было).
- На всех трёх страницах присутствуют `label-list` и `founder`.
- Детский регистр: `div.kids` обёрнут корректно, переопределение токенов собралось как `.kids[data-astro-cid-…]{--font-display:"Comfortaa"…}`. Кастомные свойства наследуются вниз независимо от скоупинга Astro, поэтому `LabelList` и `FounderNote` внутри контейнера берут детские гарнитуры — тот же механизм, что уже проверен на `--accent` в прошлых циклах.
- Изоляция детской пары подтверждена: `@font-face` для `Comfortaa`/`Nunito` присутствует только в `dist/detyam/index.html`, на `/vzroslym` их нет. Детская пара — 104 КБ, грузится только детской страницей.
- Все 8 `.woff2` в `dist/fonts/`, суммарно 228 КБ.
- Расхождение по размеру групп, которое ловило ревью прошлого цикла, снято: на `/o-studii` теперь одна формулировка «трёх-пяти человек, в детских — до четырёх» вместо двух разных чисел на разных страницах.

**Не проверено вживую** (`claude-in-chrome` недоступен — шестая сессия подряд):
- Читаются ли строки как единый список, а не как набор равнозначных полос — ради этого у первой строки граница толще, но проверить можно только глазом.
- Не разъезжается ли трёхколоночная сетка `130px | 1fr | auto` на средних ширинах, где «медиум» из двух-трёх слов может переноситься некрасиво.
- Схлопывание в один столбец на 700px и перенос возрастной метки наверх (`order: -1`).
- Как Comfortaa смотрится рядом с Bitter — на детской странице заголовок `h1` и заголовки секций теперь разной гарнитурой, чем на взрослых.
- Общий вопрос, оставшийся с цикла 1: не изменился ли зрительный размер заголовков после смены гарнитуры.

### Итог по циклу
Структурная замена выполнена и подтверждена статически: карточек больше нет ни на одной странице, регистры разведены токенами поверх общего скелета. Открытые пункты — исключительно зрительные пропорции, правятся точечно в `tokens.css` и в сетке `LabelList`.

---

## Результаты прохода: детская доска (2026-08-09)

Правка по замечаниям заказчика: на главной детская половина была набрана взрослыми шрифтами, и детский список направлений вернулся к виду приколотых карточек, одобренному во втором раунде макетов, — с местом под фотографию под каждой.

**Подтверждено статически** (`npm run build`, `npm run check`, разбор `dist/`):
- Оба гейта чистые: 7 страниц, 0 ошибок сборки, 0 ошибок и предупреждений проверки типов.
- Детская типографика на главной починена: `Comfortaa` присутствует в `dist/index.html`, класс собрался как `class="pane kid kids"`. На `/vzroslym` детских гарнитур по-прежнему ноль — изоляция не сломалась.
- Класс `.kids` вынесен в `global.css` без скоупинга, поэтому работает и на всю страницу (`/detyam`), и на половину блока (главная). `/detyam` после переноса не сломалась — обёртка на месте, `Comfortaa` подхватывается.
- На `/detyam` четыре карточки, у каждой: фото-слот, полоска скотча, у двух — возрастная метка `8+` (ровно там, где возраст назван в исходнике студии; у лепки и скетчинга его нет и он не выдуман).
- Наклон карточек детерминированный, из фиксированного набора: `-1.4°, 0.9°, -0.6°, 1.5°`. Две сборки подряд дают побайтово одинаковый HTML — вёрстка не плавает между билдами.
- Скотч помечен `aria-hidden="true"` — чисто декоративный элемент не попадает в скринридер.
- `LabelList` остался на взрослых и нейтральных страницах: «аппликационный» язык применён только к детскому регистру, как и зафиксировано в спеке.

**Не проверено вживую** (`claude-in-chrome` недоступен — седьмая сессия подряд):
- Читается ли наклон как «чуть-чуть хаотично», а не как небрежность. Грань тонкая, и исследование отдельно предупреждало, что случайная асимметрия для бренда «сделано с заботой» опаснее строгой сетки.
- Не мешает ли наклон читать текст на карточке.
- Выпрямление карточки при наведении (`rotate(0)` + подъём) — задумано как «поправили записку на доске».
- Как сетка `auto-fit, minmax(230px, 1fr)` ведёт себя на трёх карточках в ряду — наклонённые соседи могут визуально задевать друг друга.
- Общий незакрытый вопрос с цикла 1: зрительный размер заголовков после смены гарнитуры.

### Итог
Обе правки заказчика внесены и подтверждены статически. Открытые пункты — зрительные, и все правятся точечно: наклон в массиве `TILTS`, плотность сетки в `PinBoard`.

---

## Результаты прохода: переход-шторка (2026-08-09)

Третий и последний цикл пересмотра визуала. Панель «Взрослым»/«Детям» на главной разворачивается в цветную intro-секцию раздела через нативные cross-document View Transitions.

**Важная оговорка про этот цикл.** В отличие от двух предыдущих, здесь статическая проверка почти ничего не доказывает: переход существует только в рантайме браузера. Разбором `dist/` можно подтвердить, что механизм объявлен и имена связаны, но не то, что морф выглядит как задумано.

**Подтверждено статически** (`npm run build`, `npm run check`, разбор `dist/`):
- Оба гейта чистые: 7 страниц, 0 ошибок сборки и проверки типов.
- `@view-transition{navigation:auto}` присутствует в общем CSS и — проверено разбором вложенности — лежит **внутри** `@media (prefers-reduced-motion: no-preference)`. Минификатор его оттуда не вынес: те, кто просил меньше движения, получат обычную навигацию.
- Имена парные и совпадают дословно: на главной `adult-panel` и `kid-panel`; на `/vzroslym` — `adult-panel`; на `/detyam` — `kid-panel`. Несовпадение имён тихо превратило бы морф в обычный кросс-фейд, поэтому сверялось посимвольно.
- Шапка получила собственное имя `site-header` в общем CSS — чтобы не участвовать в кросс-фейде корня и не моргать при переходе.
- Длительность морфа задана `420ms` с `cubic-bezier(.65,0,.35,1)` вместо дефолтных 250мс.
- Ссылка «← На главную» присутствует на обеих страницах разделов и находится **внутри** именованного элемента перехода — значит уезжает вместе со шторкой, а не анимируется отдельно.
- Своего JS не прибавилось: единственный скрипт на странице — прежний условный загрузчик полифиллов попапа.

**Не проверено вживую** (`claude-in-chrome` недоступен — восьмая сессия подряд) — и здесь это критичнее, чем в прошлых циклах:
- Сам морф: разворачивается ли панель в секцию плавно, попадают ли границы, не «прыгает» ли содержимое внутри панели во время интерполяции.
- Скорость 420мс — подобрана вслепую, может оказаться медленной или быстрой.
- Обратный переход (со страницы раздела на главную) — должен морфить симметрично.
- Не моргает ли шапка, несмотря на собственное имя.
- Деградация в Firefox: должна быть обычная мгновенная навигация без ошибок в консоли.
- Поведение при `prefers-reduced-motion: reduce` — должна быть обычная навигация.

### Итог по циклу
Механизм объявлен корректно и связан правильно, но вердикт по самому эффекту вынести нельзя без браузера. Это первый пункт, который стоит проверить, как только появится живая сессия: настройка сводится к одному числу (`animation-duration`) и, при необходимости, к отказу от эффекта — он полностью изолирован в CSS и удаляется без следов.

## Общий итог трёх циклов пересмотра визуала

Реализовано: типографика (Bitter + Golos Text, самохостинг, интерлиньяж 1.7), замена карточной сетки на строку-этикетку и текст от первого лица, детский регистр со своей парой шрифтов и приколотыми карточками, переход-шторка. Незакрытым остаётся внешнее ограничение из спеки — реальные фотографии: слоты готовы во всех компонентах, `PhotoPlaceholder` принимает `src`, разметка при подстановке не изменится.

---

## Результаты прохода: главная, фаза A (2026-08-09)

Первый цикл наполнения главной по ресёрчу `docs/research/homepage/`. Добавлено то, что не требует фотографий: секция «Как проходит занятие» (`Steps`), анонс услуг на заказ (переиспользован `LabelList`), строка-мостик к видеоурокам, наполнена `/kontakty` (снят разрыв пути «Записаться»), `/vitrina` получила текстовую заглушку со списком услуг вместо пустой.

**Подтверждено статически** (`npm run build`, `npm run check`, разбор `dist/`):
- Оба гейта чистые: 7 страниц, 0 ошибок сборки и проверки типов.
- `/kontakty` содержит телефон `8 953 071 67 85`, ссылки на `vk.ru/kistpero` и `vk.me/kistpero` — путь «Записаться» больше не упирается в заглушку.
- На главной присутствуют `Steps`, `LabelList` (услуги) и строка-мостик; шаги пронумерованы по порядку `1, 2, 3, 4` — проверено извлечением номеров из собранного HTML, не только визуальным осмотром кода.
- Телефон не продублирован на главной — эти данные только на `/kontakty`, как и задумано.
- `/vitrina` содержит все 5 услуг из источника (роспись стен, барельеф, авторская керамика, картина под интерьер, роспись одежды), включая точные термины «Барельеф» и «Роспись одежды».
- `Steps` и `LabelList` — разные компоненты с разной семантикой (последовательность vs параллельный список), не смешаны в один с условной нумерацией.

**Не проверено вживую** (`claude-in-chrome` недоступен — девятая сессия подряд):
- Читаются ли четыре шага именно как последовательность на разных ширинах экрана, особенно на мобильном, где сетка `auto-fit` может развернуть их в один столбец, а порядковые номера — потерять ощущение «шага».
- Не путается ли `Steps` (с номерами) с `LabelList` (без номеров) при вёрстке друг под другом на одной странице — оба используют похожую типографику, разница только в наличии цифры слева.
- Общий вопрос, тянущийся с цикла 1: зрительный размер заголовков после смены гарнитуры.

### Итог по фазе A
Контент, не заблокированный фотографиями, реализован и подтверждён статически. Главный практический результат — путь «Записаться» больше не разорван. Фаза B (полоса-передышка, мозаика, портрет мастера) — отдельный будущий цикл после разметки фотографий.
